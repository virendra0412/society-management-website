import BlogPageContent from "@/components/BlogPageContent";

export const metadata = {
  title: "Blog — SocietyApp",
  description: "Tips, guides, and news for society secretaries and RWA presidents managing residential communities in India.",
};

export default function BlogPage() {
  return <BlogPageContent />;
}
